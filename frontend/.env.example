# Salin jadi frontend/.env. JANGAN commit file .env.
# CATATAN: nilai ini ikut ter-bake saat `yarn build`, jadi harus sudah benar
# SEBELUM build produksi dijalankan.
REACT_APP_BACKEND_URL=http://localhost:8001
