{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && CI=false npx craco build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
